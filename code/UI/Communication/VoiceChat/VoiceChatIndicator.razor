@using Sandbox;
@using Sandbox.UI;

@namespace Murder.UI

@attribute [StyleSheet("UI/Communication/VoiceChat/VoiceChatIndicator.scss")]
@inherits WorldPanel

<root>
	<img src="ui/voicechat.png" class="voice-icon" />
</root>

@code
{
	public VoiceChatIndicator(IClient client)
	{
		this.Enabled(UI.Hud.DisplayedPlayer.Client != client);
	}
}